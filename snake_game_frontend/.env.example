# Optional feature flags for the Snake game
# JSON object. If missing or invalid, safe defaults are used.
# Available flags:
# - wrap: boolean (enable wrap-around mode so the snake crosses walls)
REACT_APP_FEATURE_FLAGS={"wrap":false}
